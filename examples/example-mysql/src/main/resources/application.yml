server:
  port: 8080

spring:
  application:
    name: mybatis-plus-upsert-example-mysql

  datasource:
    url: jdbc:mysql://localhost:3306/example_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: admin
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

  upsert:
    enabled: true
    db-type: mysql
    use-new-mysql-syntax: false