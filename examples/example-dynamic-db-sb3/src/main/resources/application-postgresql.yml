spring:
  datasource:
    dynamic:
      primary: postgresql
      datasource:
        mysql:
          url: jdbc:mysql://localhost:33306/test?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
          username: root
          password: root@123
          driver-class-name: com.mysql.cj.jdbc.Driver
        postgresql:
          url: jdbc:postgresql://localhost:15432/test?currentSchema=public
          username: postgres
          password: pg@test
          driver-class-name: org.postgresql.Driver
